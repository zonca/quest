{
  "name": "quest-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:site",
    "build:site": "rm -rf dist && mkdir -p dist && cp *.html dist/ && cp -r images dist/ && npx cleancss -o dist/style.css style.css"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3"
  }
}

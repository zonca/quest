{
  "name": "quest-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:site",
    "build:site": "rm -rf dist && mkdir -p dist && cp *.html dist/ && cp email-obfuscate.js dist/ && cp -r images dist/ && npx clean-css-cli -o dist/style.css style.css"
  },
  "devDependencies": {
    "axios": "^1.17.0",
    "clean-css-cli": "^5.6.3"
  }
}
